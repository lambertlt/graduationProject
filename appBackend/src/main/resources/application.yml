spring:
  security:
    user:
      name: lambert
      password: lambert
  servlet:
    multipart:
      max-file-size: 2096MB
      max-request-size: 2096MB
      # 开启文件上传服务
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.94.160.237:3306/app?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: lamberts
server:
  port: 8080
mybatis:
tomcat:
  uri-encoding: UTF-8